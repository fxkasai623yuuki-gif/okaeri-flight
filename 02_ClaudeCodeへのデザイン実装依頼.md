# ChatGPT のデザインが出てきたら、Claude Code に頼む文

ChatGPT が出した HTML を `design/chatgpt.html` として保存し、方針の文章は `design/chatgpt-notes.md` に保存してから、Claude Code に次を貼ってください。

**運航データ更新のループを回しているセッションとは別のセッションで作業してください。** 公開ページの差し替えは、到着前の大事な時間帯を避けるのがおすすめです。

---

`design/chatgpt.html` と `design/chatgpt-notes.md` は ChatGPT が作った新しいデザイン案です。これをもとに `index.html` を作り直してください。

## 前提

- 現行の `index.html` は動いている本番ページです。データの読み込みと計算ロジックはこれを流用してください:
  - `status.json` を1分ごとに再取得（`?t=` でキャッシュ回避、`cache: 'no-store'`）
  - 状態判定（出発前／飛行中／乗り継ぎ中／到着）
  - `dep`/`arr` の実際・見込み時刻があれば優先し、無ければ予定時刻で計算
  - SVG path 上の飛行機位置と角度の計算（`getPointAtLength`）、通過済み区間の描画
  - タイムゾーン変換（`Australia/Perth`、`Asia/Singapore`、`Asia/Tokyo`）
  - `?t=2026-09-18T03:00:00+09:00` のような URL パラメータで時刻をプレビューする機能
  - データが45分以上古いときの注意表示
- `status.json` のスキーマは `CLAUDE.md` のとおり。変更しないでください。

## やってほしいこと

1. `design/chatgpt.html` のデザイン（色、フォント、レイアウト、状態ごとの見た目）を採用し、ChatGPT に指定した id / data-field に現行ロジックをつなぐ。
2. `body` の `data-state`（pre / flying / layover / arrived / home）、`data-delayed`、`data-stale` を実装側で切り替える。
   - `arrived` は成田着陸から90分以内、`home` はそれ以降。
   - `delayed` は、どれかの便で実際・見込み時刻が予定より11分以上遅いとき。
3. 行程データ（便名、区間、予定時刻、ターミナルなど）は `CLAUDE.md` の表と一致させる。ChatGPT のモックに事実と違う値があれば、`CLAUDE.md` 側を正とする。
4. デザイン案が外部画像や許可外の外部スクリプトを使っていたら、インラインSVGや絵文字に置き換える。
5. 座席番号や名前は載せない。

## 確認

- `?t=` で次の時刻を開き、状態ごとに表示が正しいか確認する（可能ならスクリーンショットを撮って見比べる）:
  - `2026-09-17T14:00:00+09:00`（出発前）
  - `2026-09-17T16:30:00+09:00`（QF1655 飛行中）
  - `2026-09-17T20:00:00+09:00`（パース乗り継ぎ）
  - `2026-09-18T03:00:00+09:00`（QF77 飛行中）
  - `2026-09-18T07:00:00+09:00`（シンガポール乗り継ぎ）
  - `2026-09-18T13:00:00+09:00`（JL712 飛行中）
  - `2026-09-18T16:30:00+09:00`（到着直後）
  - `2026-09-18T19:00:00+09:00`（おかえり）
- 幅375pxと1280px、ライト／ダーク両方で崩れないこと。
- 問題なければ、現行の `index.html` を `design/old-index.html` に退避してから差し替え、commit & push。
