# ChatGPT へのデザイン依頼文

以下をそのまま ChatGPT に貼ってください。

---

妹がオーストラリアから日本へ飛行機を乗り継いで帰ってくる間、家族が「いま妹はどこにいるか」をスマホで見守るための、1ページのWebサイトのデザインをお願いします。

実装（データの読み込みや位置計算）は別のAI（Claude Code）が行います。あなたにお願いしたいのは、**デザインの方針と、そのまま実装の土台にできる HTML/CSS のモック**です。

## 見る人と使い方

- 日本にいる家族（兄と両親）が、主にスマホで開きます。
- 数時間おきに何度も開き、数秒見て閉じます。長く読むページではありません。
- 開いた瞬間に「いまどこにいるか」「成田まであとどれくらいか」が分かることが最優先です。

## トーン

- かわいい雰囲気にしてください。ただし子どもっぽすぎず、大人が家族に送っても恥ずかしくない仕上がりにしてください。
- オーストラリアから日本へ帰ってくる旅らしさがほしいです（空、飛行機、南半球から北半球へ、など）。
- 避けたいもの:
  - よくあるSaaSのダッシュボード風
  - 同じ角丸カードを並べただけのレイアウト
  - クリーム色背景＋テラコッタ色
  - 黒背景＋蛍光色1色

## 行程（事実データなので変更しないでください）

| 便 | 区間 | 出発（現地） | 到着（現地） | 日本時間 |
|---|---|---|---|---|
| QF1655（カンタス） | ブルーム → パース | 9/17(木) 14:15 | 16:55 | 15:15 → 17:55 |
| パースで乗り継ぎ 6時間25分（国際線ターミナル4） | | | | |
| QF77（カンタス） | パース → シンガポール | 9/17(木) 23:20 | 9/18(金) 04:55 | 0:20 → 5:55 |
| シンガポール・チャンギ空港で乗り継ぎ 3時間10分（ターミナル1） | | | | |
| JL712（JAL運航、QF4021共同運航） | シンガポール → 成田 | 9/18(金) 08:05 | 16:10 | 9:05 → 16:10 |

成田は第2ターミナル到着。オーストラリア西部とシンガポールは日本より1時間遅れです。

## 画面に必要な要素（優先度順）

1. **いまの状態メッセージ**: 例「パース → シンガポールの空の上！」「シンガポール到着まであと2時間10分」「成田到着まであと11時間」
2. **ルート図（地図風）**: ブルーム、パース、シンガポール、成田の4地点を結ぶ。飛行機アイコンが現在地に表示され、飛行中は線の上を進む。
   - ルートは SVG の `<path>` で描き、区間ごとに `id="route-0"` `id="route-1"` `id="route-2"` を付けてください（実装側が path 上の座標に飛行機を置きます）。
   - 飛行機は `<g id="plane">` にまとめ、**右向き（0°）** で描いてください（実装側で回転させます）。
   - 地図は正確でなくてかまいません。デフォルメしたかわいい地図やイラスト風で大丈夫です。
3. **全体の進み具合**（ブルーム出発を0%、成田到着を100%として）
4. **時計を2つ**: 日本の時間と、妹がいる場所の時間
5. **行程タイムライン**: 3便と4空港を順番に。各便には便名、出発→到着（現地時刻と日本時間）、所要時間、ターミナル、ゲート、搭乗開始時刻、**運航状況バッジ**（予定どおり／◯分遅れ／飛行中／着陸済み／欠航）
6. **データの更新時刻**: 「📡 19:15 に運航データを反映」。データが古いときの注意表示も
7. **各便の運航状況を外部サイトで調べるリンク**

## デザインしてほしい状態（全部）

- A: 最初の便の出発前
- B: 飛行中（メインのモックはこの状態で作ってください）
- C: 空港で乗り継ぎ中
- D: 遅延が出ている
- E: 成田に着陸した直後（入国審査や荷物受け取り中かも）
- F: 到着して落ち着いたあと（「おかえり！」）
- G: 運航データが古い、または取れていない

状態の切り替えは `<body data-state="pre|flying|layover|arrived|home">` と、遅延は `data-delayed="true"`、データが古いときは `data-stale="true"` のような属性で表現できるようにしてください。

## 技術的な条件

- 1ファイルの `index.html`。CSS は `<style>` 内に書く。JavaScript は不要です（状態切り替えのデモ用ボタン程度ならOK）。
- 外部画像は使えません。アイコンは絵文字かインラインSVGにしてください。
- Webフォントは Google Fonts のみ使えます。日本語フォントのフォールバックも指定してください。
- モバイルファースト（幅375px基準）で、PCで開いても崩れないようにしてください。
- ダークモードに対応してください（`prefers-color-scheme`）。色は CSS 変数で管理してください。
- アニメーションは控えめにし、`prefers-reduced-motion` に対応してください。
- 座席番号や名前などの個人情報は載せないでください（公開URLになるため）。

## 動的に書き換える場所の id（この名前で付けてください）

- `#status-emoji` 状態の絵文字
- `#status-title` 状態メッセージ
- `#status-sub` 補足（次の到着まであと◯分など）
- `#eta-narita` 成田到着まであと◯時間◯分
- `#updated-at` データ更新時刻の表示
- `#route-0` `#route-1` `#route-2` ルートの path（未通過部分）
- `#route-done-0` `#route-done-1` `#route-done-2` 通過済み部分の path（同じ d 属性を重ねる想定）
- `#plane` 飛行機
- `#airport-BME` `#airport-PER` `#airport-SIN` `#airport-NRT` 地図上の空港ピン（通過済みは `.reached` クラス）
- `#progress-fill` 進み具合バー
- `#clock-jst` `#clock-local` `#clock-local-label`
- 行程の各便カード: `[data-leg="0"]` `[data-leg="1"]` `[data-leg="2"]`。その中に `[data-field="dep-time"]` `[data-field="arr-time"]` `[data-field="jst"]` `[data-field="duration"]` `[data-field="terminal"]` `[data-field="gate"]` `[data-field="board"]` `[data-field="badge"]` `[data-field="status-link"]`
- 現在の便・空港には `.is-now`、通過済みには `.is-done` クラス

## 出してほしいもの

1. デザイン方針（短く）: 色5〜6色（名前とHEX）、フォントと使い分け、レイアウトの考え方、このページならではのこだわりを1〜2点
2. `index.html` の全文（状態B「QF77で飛行中、シンガポール到着まであと2時間10分」を表示した状態）
3. 状態A〜Gで、見た目がどう変わるかの説明と、そのためのCSS
4. 上の id / data-field をすべて使っているかのチェックリスト
