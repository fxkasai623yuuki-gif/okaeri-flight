# Claude Code に貼るループ用コマンド

このフォルダ（リポジトリ）で `claude` を起動して、次の1行を貼る。

```
/loop 15m CLAUDE.md の「運航データ更新手順」を1回実行して。変化があったときだけ status.json を commit & push し、結果を1行で報告して。
```

- 15分ごとに実行される。止めたいときはセッション内で「ループを止めて」と伝える。
- PC がスリープするとループも止まるので、到着（9/18 16:10 JST）の2時間後くらいまでスリープしない設定にしておく。
- 最初の1回だけは手で「CLAUDE.md の運航データ更新手順を1回実行して」と頼んで、ちゃんと読めて push できるか確認するのがおすすめ。
